pkg update && pkg upgrade -y && pkg install git curl wget tree neovim nano vim proot-distro python openssh nodejs termux-api gh zsh rust neofetch -y && git clone https://github.com/LazyVim/starter ~/.config/nvim && rm -rf ~/.config/nvim/.git && chsh -s zsh && RUNZSH=no CHSH=no sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended && git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k && mkdir -p ~/.termux && printf "extra-keys = [['ESC','/','-','HOME','UP','END','PGUP'],['TAB','CTRL','ALT','LEFT','DOWN','RIGHT','PGDN']]\n" > ~/.termux/termux.properties && termux-reload-settings && echo 'ZSH_THEME="powerlevel10k/powerlevel10k"' >> ~/.zshrc && echo 'alias up="pkg update && pkg upgrade -y"' >> ~/.zshrc && echo 'alias in="pkg install -y"' >> ~/.zshrc && echo 'neofetch --ascii_distro kali 2>/dev/null || true' >> ~/.zshrc && echo 'cl() { clear; printf "┌──(%s@setup)-[%s]-[%s]\n└─$ " "$USER" "$(date "+%H:%M:%S")" "${PWD/#$HOME/~}"; }' >> ~/.zshrc && echo 'alias cls=cl' >> ~/.zshrc && echo 'alias debian="proot-distro login debian"' >> ~/.zshrc && proot-distro install debian

